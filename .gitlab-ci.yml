stages:
  - build

build:
  stage: build
  image: gradle:5.2-jre11-slim
  script:
    - java -version
    # - chmod +x gradlew
    - gradle build
  artifacts:
    reports:
      junit:
        - build/test-results/test/TEST-*.xml
    paths:
      - build/reports/jmh
      - build/reports/tests
